import React from 'react'
import "./SendButton.css"

function SendButton( { title } ) {
  return (
    <div>
         <button
                className="btn"
                onClick={() => {
                }}
            >Criar</button>
    </div>
  )
}

export default SendButton